# k6 Load Test Results — Week 17, 2026

Results are generated by `benchmarks/run_k6.sh` and committed here after each run.

## How to generate results

```bash
# Start the app first (Docker Compose or local)
docker compose up -d

# Run all three k6 tests
./benchmarks/run_k6.sh

# Or target staging
BASE_URL=https://your-staging-url.render.com ./benchmarks/run_k6.sh
```

## Scripts

| Script | Scenario | Target |
|--------|----------|--------|
| `qualification_load.js` | 50 VUs ramp over 30s | P95 < 2s, error < 1% |
| `burst.js` | Spike 10 → 500 VUs | error < 5% at peak |
| `sustained.js` | 150 req/s constant arrival for 10 min | P95 < 2s, error < 1% |

## Status

Results pending — scripts committed 2026-04-29, awaiting staging run.

Do **not** quote numbers from `benchmarks/bench_cache.py` (simulation) in
case studies or interviews. Use only numbers from JSON files in this directory.
