Archived Streamlit components and dashboards not reachable from app.py, pages/, navigation/ (including hub_dispatch lazy imports), or any test as of 2026-06-11.
Kept out of the import path to cut sprawl; restore with `git mv` if a component is needed again.
