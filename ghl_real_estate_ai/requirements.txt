# GHL Real Estate AI - Production Dependencies
# Compatible with Python 3.10, 3.11, 3.12, 3.14
# Tested on: Linux (amd64), macOS (arm64/Intel)

# FastAPI + ASGI Server
fastapi==0.128.0
uvicorn[standard]==0.39.0
pydantic==2.12.5
pydantic-settings==2.11.0

# Anthropic Claude SDK
anthropic==0.76.0

# Vector Database & Embeddings (pinned for stability)
chromadb==0.4.24
sentence-transformers==2.3.1
onnxruntime==1.20.1; sys_platform == 'linux' or (sys_platform == 'darwin' and platform_machine == 'x86_64')
onnxruntime-silicon==1.16.3; sys_platform == 'darwin' and platform_machine == 'arm64'

# HTTP Client (for GHL API calls)
httpx==0.28.1
requests==2.32.5

# Utilities
python-dotenv==1.2.1
python-multipart==0.0.20

# Numpy (compatible with all platforms)
numpy==1.26.3

# Security Dependencies
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4

# UI & Dashboard
streamlit==1.50.0
pandas==2.3.3
plotly==5.18.0

# Additional ML dependencies
scikit-learn==1.6.1
tokenizers==0.22.2

# Real-time Features & WebSocket Support
redis==5.0.1
websockets==12.0
websocket-client==1.9.0
aioredis==2.0.1

# Async Support
asyncio-mqtt==0.16.2
