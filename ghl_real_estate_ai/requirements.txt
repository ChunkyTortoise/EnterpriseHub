# GHL Real Estate AI - Production Dependencies
# Compatible with Python 3.10, 3.11, 3.12, 3.14
# Tested on: Linux (amd64), macOS (arm64/Intel)

# FastAPI + ASGI Server
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0

# Anthropic Claude SDK
anthropic==0.18.1

# Vector Database & Embeddings (pinned for stability)
chromadb==0.4.24
sentence-transformers==2.3.1
onnxruntime==1.16.3; sys_platform == 'linux' or (sys_platform == 'darwin' and platform_machine == 'x86_64')
onnxruntime-silicon==1.16.3; sys_platform == 'darwin' and platform_machine == 'arm64'

# HTTP Client (for GHL API calls)
httpx==0.26.0
requests==2.31.0

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.6

# Numpy (compatible with all platforms)
numpy==1.26.3

# Security Dependencies
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# UI & Dashboard
streamlit==1.30.0
pandas==2.1.4
plotly==6.5.1

# Additional ML dependencies
scikit-learn==1.4.0
tokenizers==0.15.1
