"""Prompt registry tests.

The DB-backed PromptRegistry was replaced in Wave 2 (ticket 788i) with a
YAML-backed loader at ghl_real_estate_ai/services/prompt_registry.py.

Tests for the current implementation live in:
    tests/unit/test_prompt_registry.py
"""
