# Benchmark Evidence

This folder stores reproducible benchmark artifacts for public claims.

## How to reproduce

```bash
cd /Users/cave/Projects/EnterpriseHub_new/agentforge
PYTHONPATH=. python3 scripts/benchmark_engine.py --runs 100 --output evidence/benchmarks/latest.json
```

## Latest verified artifact

- `evidence/benchmarks/2026-02-20.json`

All benchmark claims in docs should reference an artifact in this folder.
