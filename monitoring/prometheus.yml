# Prometheus Configuration for Jorge's Revenue Platform
# Application and business metrics monitoring
# Version: 1.0.0

global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: jorge-revenue-production
    environment: production

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

# Load rules once and periodically evaluate them
rule_files:
  - "/etc/prometheus/rules/*.yml"

# Scrape configurations
scrape_configs:
  # ============================================
  # Application Metrics
  # ============================================
  - job_name: 'jorge-revenue-api'
    kubernetes_sd_configs:
      - role: pod
        namespaces:
          names:
            - jorge-revenue-platform
    relabel_configs:
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
        action: keep
        regex: true
      - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_path]
        action: replace
        target_label: __metrics_path__
        regex: (.+)
      - source_labels: [__address__, __meta_kubernetes_pod_annotation_prometheus_io_port]
        action: replace
        regex: ([^:]+)(?::\d+)?;(\d+)
        replacement: $1:$2
        target_label: __address__
      - action: labelmap
        regex: __meta_kubernetes_pod_label_(.+)
      - source_labels: [__meta_kubernetes_namespace]
        action: replace
        target_label: kubernetes_namespace
      - source_labels: [__meta_kubernetes_pod_name]
        action: replace
        target_label: kubernetes_pod_name

  # ============================================
  # Business Metrics
  # ============================================
  - job_name: 'business-metrics'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/business'
    scrape_interval: 60s

  # ============================================
  # Infrastructure Metrics
  # ============================================
  - job_name: 'kubernetes-nodes'
    kubernetes_sd_configs:
      - role: node
    relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_node_label_(.+)

  - job_name: 'kubernetes-pods'
    kubernetes_sd_configs:
      - role: pod
    relabel_configs:
      - action: labelmap
        regex: __meta_kubernetes_pod_label_(.+)
      - source_labels: [__meta_kubernetes_namespace]
        action: replace
        target_label: kubernetes_namespace
      - source_labels: [__meta_kubernetes_pod_name]
        action: replace
        target_label: kubernetes_pod_name

  # ============================================
  # Jorge AI Bot Ecosystem Metrics
  # ============================================
  - job_name: 'jorge-seller-bot'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/bots/seller'
    scrape_interval: 30s

  - job_name: 'jorge-lead-bot'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/bots/lead'
    scrape_interval: 30s

  - job_name: 'jorge-intent-decoder'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/bots/intent'
    scrape_interval: 30s

  - job_name: 'jorge-ml-pipeline'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/ml'
    scrape_interval: 15s

  # ============================================
  # Conversation Intelligence Metrics
  # ============================================
  - job_name: 'claude-conversation-intelligence'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/conversation'
    scrape_interval: 30s

  # ============================================
  # Property Alert Engine Metrics
  # ============================================
  - job_name: 'property-alert-engine'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/alerts'
    scrape_interval: 60s

  # ============================================
  # Health Check Metrics
  # ============================================
  - job_name: 'health-checks'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/health'
    scrape_interval: 30s

  # ============================================
  # External API Performance Metrics
  # ============================================
  - job_name: 'ghl-api-performance'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/external/ghl'
    scrape_interval: 60s

  - job_name: 'claude-api-performance'
    static_configs:
      - targets: ['jorge-revenue-api-svc:8000']
    metrics_path: '/metrics/external/claude'
    scrape_interval: 60s

  # ============================================
  # Database Metrics
  # ============================================
  - job_name: 'postgres-exporter'
    static_configs:
      - targets: ['postgres-exporter:9187']

  - job_name: 'redis-exporter'
    static_configs:
      - targets: ['redis-exporter:9121']
