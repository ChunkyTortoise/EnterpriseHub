apiVersion: apps/v1
kind: Deployment
metadata:
  name: enhanced_ml_integration
  namespace: ai-operations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enhanced_ml_integration
  template:
    metadata:
      labels:
        app: enhanced_ml_integration
    spec:
      containers:
      - env:
        - name: ENVIRONMENT
          value: production
        - name: CONFIG_FILE
          value: /config/enhanced_ml_integration.json
        image: ai-operations/enhanced_ml_integration:latest
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
        name: enhanced_ml_integration
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 1Gi
          requests:
            cpu: 250m
            memory: 512Mi
        volumeMounts:
        - mountPath: /config
          name: config
      volumes:
      - configMap:
          name: enhanced_ml_integration-config
        name: config
