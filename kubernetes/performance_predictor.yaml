apiVersion: apps/v1
kind: Deployment
metadata:
  name: performance_predictor
  namespace: ai-operations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: performance_predictor
  template:
    metadata:
      labels:
        app: performance_predictor
    spec:
      containers:
      - env:
        - name: ENVIRONMENT
          value: production
        - name: CONFIG_FILE
          value: /config/performance_predictor.json
        image: ai-operations/performance_predictor:latest
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
        name: performance_predictor
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 1Gi
          requests:
            cpu: 250m
            memory: 512Mi
        volumeMounts:
        - mountPath: /config
          name: config
      volumes:
      - configMap:
          name: performance_predictor-config
        name: config
