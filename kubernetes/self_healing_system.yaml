apiVersion: apps/v1
kind: Deployment
metadata:
  name: self_healing_system
  namespace: ai-operations
spec:
  replicas: 1
  selector:
    matchLabels:
      app: self_healing_system
  template:
    metadata:
      labels:
        app: self_healing_system
    spec:
      containers:
      - env:
        - name: ENVIRONMENT
          value: production
        - name: CONFIG_FILE
          value: /config/self_healing_system.json
        image: ai-operations/self_healing_system:latest
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 60
          periodSeconds: 30
        name: self_healing_system
        ports:
        - containerPort: 8000
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 1Gi
          requests:
            cpu: 250m
            memory: 512Mi
        volumeMounts:
        - mountPath: /config
          name: config
      volumes:
      - configMap:
          name: self_healing_system-config
        name: config
