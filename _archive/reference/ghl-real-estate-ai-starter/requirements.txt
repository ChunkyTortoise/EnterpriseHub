# GHL Real Estate AI - Python Dependencies
# FastAPI + ASGI Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Anthropic Claude SDK
anthropic==0.18.1

# Vector Database & Embeddings
chromadb==0.4.22
sentence-transformers==2.3.1

# Database (PostgreSQL)
psycopg2-binary==2.9.9
sqlalchemy==2.0.25
alembic==1.13.1

# Redis (Session State & Caching)
redis==5.0.1
hiredis==2.3.2

# HTTP Client (for GHL API calls)
httpx==0.26.0
requests==2.31.0

# Background Tasks
celery==5.3.6
# For Celery broker (if using Redis)
celery[redis]==5.3.6

# Utilities
python-dotenv==1.0.0
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing (if needed)
python-multipart==0.0.6  # Form data parsing

# Logging & Monitoring
structlog==24.1.0
sentry-sdk[fastapi]==1.40.0  # Optional: Error tracking

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
httpx==0.26.0  # For testing async HTTP

# Code Quality
black==24.1.1
flake8==7.0.0
mypy==1.8.0
